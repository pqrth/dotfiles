# Sourced by .zshrc but never tracked: holds live credentials.
.secret

# Generated by .zshrc from .zsh_plugins.txt on first run.
.zsh_plugins.zsh

# Compiled zsh bytecode.
*.zwc

# Finicky is a GUI browser router; its config is dead weight without the app.
# Machines with Finicky installed (the personal Mac) still get ~/.finicky.js.
{{ if not (stat "/Applications/Finicky.app") -}}
.finicky.js
{{ end -}}
