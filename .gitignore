# This is the REPOSITORY's own ignore file.
# The payload installed to ~/.gitignore lives at home/dot_gitignore.

# macOS folder-view metadata
.DS_Store
._*
.Spotlight-V100
.Trashes

# Claude Code machine-local settings
.claude/settings.local.json
